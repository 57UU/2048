<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="_2048.MainPage">

    <VerticalStackLayout>
        <Label x:Name="score" Text="????" HorizontalOptions="Center" FontSize="Large"/>
        <Grid x:Name="graph" HorizontalOptions="Center">
            
            
        </Grid>
        <VerticalStackLayout x:Name="childLayout">
            <Label HorizontalOptions="Center" x:Name="max"/>
            <Entry x:Name="entry" Placeholder="Focus to Type" BackgroundColor="Gold" />
            
            <HorizontalStackLayout HorizontalOptions="CenterAndExpand">
                <Button HorizontalOptions="CenterAndExpand"  Text="——————Replay——————" Clicked="Button_Clicked"/>
                <Button  HorizontalOptions="End" Text="Reset Max Score" Clicked="Button_Clicked_1"/>
            </HorizontalStackLayout>
            
        </VerticalStackLayout>
        
    </VerticalStackLayout>

</ContentPage>
